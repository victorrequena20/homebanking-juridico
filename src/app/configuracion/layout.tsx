import React from "react";
import ConfigurationLayout from "@/components/Layouts/Configuration.layout";

export default function layout({ children }: any) {
  return <ConfigurationLayout>{children}</ConfigurationLayout>;
}
